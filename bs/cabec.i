/**
message program-name(1) program-name(2). PAUSE 1 NO-MESSAGE.
**/

{admcab.i {1}}

def new global shared var sPROGRAMA     as char.

def new global shared var simpress as char.

define new global shared variable varqsai  as char format "x(14)".

def var vmercacod as char format "x(13)" label "Produto".
{sys/cabec.i}

{sys/funcoes.i}
